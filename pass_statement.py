for i in range(1000):
    pass
i = 1
while (i<1000):
    print(i)
    i+=1
# This code uses a `for` loop with a `pass` statement, which does nothing for each iteration,
# and then uses a `while` loop to print numbers from 1 to 999.
# The `while` loop increments `i` by 1 until it reaches 1000