
# This code imports the `employee` class from the `oop` module and creates an instance of the class. It then calls the `info` method to print the employee's details, including a new attribute `brother` that is added to the instance.
list= [1, 2, 3, 4, 5]
blank_list = []
for i in list:
    blank_list.append(i*i)
print(blank_list)
    